# Core
discord.py>=2.3.0
google-generativeai>=0.8.0
discord-ext-voice-recv>=0.5.0
SpeechRecognition>=3.10.0
edge-tts>=6.1.0
python-dotenv>=1.0.0
aiohttp>=3.9.0

# Voice (pilih salah satu atau semua)
discord.py[voice]>=2.3.0     # Support voice channel (butuh ffmpeg)
pyttsx3>=2.90                 # TTS offline (gratis, kualitas biasa)
gTTS>=2.5.0                  # Google TTS (butuh internet, kualitas bagus)
elevenlabs>=1.0.0             # ElevenLabs TTS (berbayar, kualitas terbaik)

# Image generation
Pillow>=10.0.0                # Image processing

# Opsional tapi direkomendasikan
PyNaCl>=1.5.0                # Untuk voice encryption
